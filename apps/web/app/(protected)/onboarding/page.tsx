"use client"
import { useAuth } from '@/features/auth/context/auth-context';
import React from 'react'

const page = () => {
  return (
    <div>
      onboarding page
    </div>
  )
}

export default page
